# BrainGrid MCP Rule Set

- Use BrainGrid tools according to user intent, not rigid command names.
- Discover project context automatically and reuse `.braingrid/project.json`.
- Route requests correctly across create, capture, refine, break down, build, task retrieval, task updates, and acceptance review.
- After analysis is done, continue the BrainGrid workflow to the next concrete action unless the user explicitly requests analysis only.
- Use `F:\Repos\GitHub\eboss-monorepo\BRAINGRID_MCP.md` as the detailed repo-local reference.

