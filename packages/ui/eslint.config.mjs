// TODO(EBOSS-111, 2026-03-08): This package currently contains only an empty
// index.ts stub. The config intentionally specifies no rules; extend with
// typescript-eslint recommended rules once real component code is added.
export default [
  {
    files: ["src/**/*.ts", "src/**/*.tsx"],
  },
];
